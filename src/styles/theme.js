const breakpoints = ['57.6rem', '76.8rem']

const colors = {
  primary: '#e0a080',
  secondary: '#441044',
}

const fonts = {
  sansSerif: '"Topaz", sans-serif',
}

const space = [
  0,
  '0.1rem',
  '0.2rem',
  '0.4rem',
  '0.8rem',
  '1.6rem',
  '3.2rem',
  '6.4rem',
]

const theme = {
  breakpoints,
  colors,
  fonts,
  space,
}

export default theme
